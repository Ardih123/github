package Ficha4;

interface Descontavel {

	/*
	 * +Ex. 3: Toda a fruta a peso e toda a fruta a volume podem ter um desconto de
	 * uma determinada percentagem. Crie uma interface Descontavel que tem um método
	 * double descontar(double percentagem) que será implementado pela classe
	 * FrutaPeso e pela class FrutaVolume. Faça as alterações necessárias nestas
	 * classes. Escreva a função main() para testar as respetivas alterações.
	 */
	
	public double descontar(double aPercentagem);
}
